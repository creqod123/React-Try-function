// import browser from 'webextension-polyfill';
// browser.runtime.onMessage.addListener((message, sender, sendResponse) => {
//     if (message.action === 'GREETING') {
//         sendResponse({ reply: `Hello, ${message.data.name}!` });
//     }
// });